# Changelog

Формат — по [Keep a Changelog](https://keepachangelog.com/ru/1.1.0/); версии — [SemVer](https://semver.org/lang/ru/).

## [0.3.1] — 2026-07-15

Скриншоты в конспекте: структурные слайды перестали теряться, кадры слайдов — обрезаться, а сборка — молча собирать старую версию. Всё найдено на реальном прогоне (тот же 99-минутный эфир) по замечаниям после выдачи.

### Изменено
- **Структурные слайды видео — обязательные скриншоты, а не «на выбор».** Слайд-оглавление метода («метод из N ступеней», «что будет») и пронумерованные слайды-заголовки этапов — это каркас конспекта; их надо вставлять все, не подменяя «более интересными» демо. Раньше каркас-слайд и слайды двух ступеней были заменены демонстрациями — и конспект потерял опору, которую зритель ждал по видео.
- **Кадр слайда проверяется строго.** При отборе скриншота (его и так открывают глазами) теперь требуется: заголовок и ключевой текст видны полностью, слайд показан на весь экран (а не в окне браузера, где панель вкладок режет верх заголовка), курсор и вебки не поверх контента. «Почти видно» не годится — берётся другой момент показа (слайд в лекции показывают несколько раз, есть и полноэкранный).

### Исправлено
- **Скриншот слайда мог быть обрезан панелью браузера.** Финальный кадр брался по `span_end` — последнему кадру серии дедупа; если к концу показа слайд свернули в окно, `span_end` ловил уже не слайд, а полноэкранный кадр оставался в начале серии. Теперь для слайдов кадр сверяется, и при обрезке переизвлекается по началу/середине серии.
- **Пересборка конспекта могла молча оставить старую версию.** При правке уже готового конспекта `inline_images.py` падал, если временный `digest.raw.html` был удалён уборкой, а подавленный вывод скрывал ошибку — на диске оставался прежний файл. Инструкция теперь требует сверять результат по факту (время файла, число встроенных картинок) и пересобирать исходник перед повторной вклейкой.

## [0.3.0] — 2026-07-12

Вёрстка конспекта: навигация перестала ломаться на длинных конспектах, ссылки стали живыми. Всё найдено на реальном прогоне (99-минутный эфир, 14 глав) и проверено в браузере.

### Добавлено
- **Внешние ссылки в конспекте кликабельны** и открываются в новой вкладке (`target="_blank"`, `rel="noopener noreferrer"`): сайты инструментов, каталоги и первоисточник в подвале. Раньше домены были просто жирным текстом — прочитать можно, нажать нельзя. Адреса берутся только из кадров или от пользователя, не выдумываются; внутренние якоря и адреса внутри блоков кода не трогаются.

### Изменено
- **Оглавление стало вертикальным вместо горизонтальной полосы глав.** Полоса упиралась в ширину экрана (потолок — около 16 глав), переполнялась, автоматически уезжала вправо и **прятала первые главы**, а листать её мышью нечем. Вертикальный список растёт вниз и переживает любое их число: на широком экране — закреплённый слева от текста, в пустом поле; на узком — кнопка «☰ Главы» с выпадающей панелью и текущей главой в шапке. Не влез по высоте — скроллится внутри себя. Проверено на 40 главах.

### Исправлено
- **Страница сопротивлялась скроллу.** Подсветка активной главы вызывала `scrollIntoView`, а при `scroll-padding-top` (отступ под липкую шапку) браузер считал ссылку внутри шапки недовиденной и откручивал **сам документ**: замерено −65 px на каждое срабатывание — пользователь крутит вниз, страница отматывает назад. Позицию списка теперь двигаем вручную, документ не трогаем.
- **Подсветка главы врала:** `IntersectionObserver` отдавал ту секцию, что последней попала в события, а не ту, что под шапкой (вверху страницы горела глава из середины). Активная глава считается по позиции, на `scroll` (passive + throttle через `rAF`).
- **Раскладка скакала при загрузке картинок:** у `<img>` не было `width`/`height`, и страница пересчитывалась по мере декодирования — на конспекте с десятком кадров 1920×1080 это било по скроллу. Размеры проставляются, декодирование ленивое и асинхронное.

## [0.2.0] — 2026-07-11

Переработка: из «посмотреть видео и ответить в чат» скилл стал «посмотреть видео и отдать самодостаточный конспект».

### Добавлено
- **Самодостаточный HTML-конспект.** Текст-ядро из локального Whisper-транскрипта; скриншоты добавляются только там, где надо посмотреть на экран. Тёмная/светлая тема, кликабельные скрины, глоссарий.
- **Установка одной командой** — `python scripts/install.py`: определяет железо (NVIDIA / Intel Arc / Apple Silicon / AMD / CPU), подбирает Whisper-бэкенд и размер модели под память, показывает план, спрашивает подтверждение и ставит. `--dry-run`, чтобы просто посмотреть.
- **pHash-дедуп кадров** (`dedup.py`): почти одинаковые кадры схлопываются в серию. Представитель серии — её **последний** кадр: применённое состояние (тумблер включён, слайд дорисован), а не начальное.
- **Vision fan-out субагентами**: кадры разбираются пачками в отдельных агентах, которые возвращают текстовый журнал состояний экрана. Основной контекст не забивается картинками.
- **Плотная перенарезка demo-зон** (`dense_zones.py`): по журналу находятся участки быстро меняющегося экрана и перерезаются гуще. Ограничено бюджетом кадров — на live-coding скилл честно откажется, а не сожжёт токены.
- **Три Whisper-бэкенда**: `faster` (NVIDIA/CPU), `ov` (Intel Arc, готовая OpenVINO-модель), `whispercpp` (Apple Silicon/AMD).

### Изменено
- **Равномерная `fps`-нарезка вместо `mpdecimate`** и жёсткого лимита кадров: плотность привязана к длине видео, потолка нет.
- **Финальные скриншоты переизвлекаются в родном разрешении**, а не берутся из пережатых vision-кадров.
- `WATCH_RES_OVERVIEW` — теперь **потолок** ширины, а не цель: кадры не апскейлятся (растянутый кадр стоит столько же токенов и не добавляет деталей).
- Язык транскрипции по умолчанию — `ru` (`task=transcribe`, без перевода).

### Исправлено
- Падение Whisper больше не глотается: раньше отчёт врал «нет аудио» и выходил с кодом 0, из-за чего конспект собирался из одних кадров. Теперь — явная ошибка и ненулевой код.
- Окно `--start/--end` режет декодирование, а не фильтрует кадры после нарезки (30-секундное окно на 25-минутном видео резало всё видео).
- `FW_DEVICE=cuda` прописывается явно: значение `auto` определяет CUDA через `import torch`, а torch не в зависимостях — транскрипция молча уезжала на CPU.
- CTranslate2 находит cuBLAS/cuDNN, установленные pip: DLL из `site-packages` регистрируются через `add_dll_directory` (иначе на Windows — `Could not locate cudnn_ops64_9.dll`).
- Preflight ловит отсутствующий бэкенд и несуществующие пути **до** закачки видео.

## [0.1.0]

Первая версия: `mpdecimate`-нарезка кадров, локальный Whisper, ответ в чат.
